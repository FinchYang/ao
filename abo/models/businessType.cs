namespace mvc104.models
{
    public enum businessType
    {
        ChangeLicense, delay, lost, damage, overage,
        expire, changeaddr, basicinfo, first, network, 
        three, five,inspectDelay,bodyDelay
    };
}