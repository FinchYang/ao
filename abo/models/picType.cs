namespace mvc104.models
{
    public enum picType
    {
        id_front, id_back, id_inhand, delay_pic, driver,
        health, overage, expire, hukou_pic, sign_pic, 
        declaration_sign,face1,face2,face3
    };
}